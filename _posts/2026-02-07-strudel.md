---
layout: post
title: "Some Strudel tips"
date: 2026-02-15
---

I've been doing a bunch of livecoding/algorave stuff in [Strudel](https://strudel.cc), and it's great fun. You can make all kinds of fun sounds through experimentation, and the docs are fantastic, but I've been itching to make a kind of "guide" that catalogues certain winning formulas for electronic music that I frequently turn to.

Here are some scattered thoughts that would make it into such a guide. Each code block is executable: press `Ctrl+Enter` to start audio and `Ctrl+.` (period) to stop.

<style>.cm-editor { font-size: 13px; }</style>

## Hi-hats

Many "house-y" rhythms sound good with `hh*16`. Just don't make them too loud. It sounds good to play them "softly, then a bit louder" on each beat.

<script src="https://unpkg.com/@strudel/repl@latest"></script>
<strudel-editor>
  <!--
// Press Ctrl+Enter. Then uncomment the
// ".gain(...)" and press Ctrl+Enter again.
// Finally, press Ctrl+. (period) to stop.

$: s("bd [bd,cp] bd [bd,cp]")
$: s("hh*16") // .gain("[0.2 0.6]*4")
-->
</strudel-editor>

(Also, claps and snares like to be on beats 2 and 4.)

## Ribbon

If you play random notes in a scale, it sounds, well, random. But if you loop a single random measure, it will start to sound like music! In the words of Adam Neely: _repetition legitimizes._ In Strudel, the _ribbon_ function `.rib` can slice out a bit of a (random) pattern and loop it.

<strudel-editor>
<!--
// Uncomment the .rib(52,1) to repeat the 52th
// random measure over and over again.
// Try changing 52 (for a different pattern), or
// 19 (for a different range of random notes)

$: s("- sh - [sh - - sh]")
$: n(irand(19).seg(16)).scale("e:minor")
   .delay(0.2) // .rib(52,1)
$: note("<c2 d2 e2!2>")

// Remember: press Ctrl+. to stop playback.
-->
</strudel-editor>

You can even do things like `.rib("52 52 52 <52 3>", 1)` to change the pattern up every other bar.

## Randomize everything

Most parameters sound fun when randomized over a small range. It can make even a single note sound interesting. Also, randomly pitch notes up or down by an octave! This kind of "accent" with `rarely` sounds fun.

<strudel-editor>
  <!--
$: sound("tr909_bd:3*4")

// Try replacing $: by _$: here,
$: note("f2*16").sound("saw")
     .fm(1)
     .lpf(1500)
     .lpenv(8)
     .lpdecay(0.02)
     .clip(0.5)
     .gain(0.9)

// and _$: by $: here, then Ctrl-Enter.
_$: note("f2*16").sound("saw")
     .fm(rand.range(0, 2))
     .lpf(rand.range(1000,2000))
     .lpenv(rand.range(4,14))
     .lpdecay(rand.range(0, 0.04))
     .clip(rand.range(0.3, 0.9))
     .gain(rand.range(0.8, 1.0))
     .swingBy(0.1, 8)
     .rarely(x=>x.transpose(choose(-12, 12)))
-->
</strudel-editor>

You can combine this with `rib`, but you don't have to.

## Chords, bass, drums, melody

This is a solid four-ingredient recipe for making a tune sound "full."

Try muting parts with `_$` to see what it feels like to have
"no chords" or "no bass" for a bit. It's fun to DJ like this.

<strudel-editor>
<!--
// Chords
// Muting me sounds "dry".
$: chord("<Dm9 Em9>").anchor("70").voicing()
    .pan(0.7).clip(0.90).gain(0.4).lp(3000)
$: chord("<Dm9 Em9>").anchor("80").voicing()
    .pan(0.3).clip(0.95).gain(0.4).lp(2000)

// Bass
// Muting me sounds "floaty".
$: note("<[d2 e2]*4 [a1 c2]*4>")
    .struct("x - x(1,4,1) x*2").sound("gm_koto,saw")

// Drums
// Muting me sounds "contemplative".
$: s("sh*16").swingBy(0.1,8).gain("[0.5 0.7]*4")
$: s("bd:6*4, - rim - rim, -!7 hh:3 -!8").clip(1)

// Melody
$: note("-!8 - c6 b5 g5 c6 b5 g5 c6 b5 g5 e5@2 -!12").slow(2)
    .vib(0.2).swingBy(0.1,8).clip(rand.range(0.5, 1))
    .pan(0.4).fm(rand).delay(0.2)
-->
</strudel-editor>

(Oh! I like this trick for making nice and "full" chord voicings: play the same chords at two different anchors, then pan them left and right.)

## Fun samples

There are loads of fun samples in `github:tidalcycles/dirt-samples` and `github:yaxu/clean-breaks`.

<strudel-editor>
<!--
samples('github:tidalcycles/dirt-samples')
setcpm(33)

// A cool pad
$: note("db1").sound("ade:3").begin(0.2).clip(1).gain(0.3)

// Nice bass
$: note("ab1 - - eb2 - - <bb1 bb2>? -").sound("bass:0")

// Many drums, e.g.
$: sound("house*4").gain(0.7)

// I found this great little house chord!
$: note("c2 - - <- - - g2>").s("house:5").delay(1)

// Lots and lots of weird little one-shots.
$: n(irand(100)).struct("x*16").sound("yeah,subroc3d").clip(1).gain(0.2)
-->
</strudel-editor>

Meanwhile, `github:yaxu/clean-breaks` catalogs some nice drum loops for breakbeats.

I like using Vocaloid samples in Strudel... There are some excellent stems licensed under BY-NC-SA on [Jamie Paige's stems page](https://jamies.page/stems), from which I took the _Strawberry_ vocal for `teto` in `github:lynn/sounds/main/vocaloid`.

<strudel-editor>
<!--
samples('github:lynn/sounds/main/vocaloid')
samples('github:yaxu/clean-breaks')
setcpm(28)

// Teto sounds good when chopped up.
// Always use clip() or she'll keep singing!
// I use begin() to make her sing random ribboned notes.
// What happens if you rand.seg(4)?
$: sound("teto").struct("x - x x - - x*2 x x x@3 x x x x")
    .begin(rand.seg(16).rib(53,1)).clip(0.9).swingBy(0.1,8).late(0.005)

// The breaks are incredibly generous ingredients, and
// basically anything you do with them sounds cool.
// Use slow()+fit() to match your bpm.
_$: sound("movement").slow(2).fit()
    .compressor("-20:20:10:.002:.02").postgain(1.1)

// Some of the gm_ sounds can sound pretty good in context.
_$: chord("<AM7 EM7>").voicing().sound("gm_string_ensemble_1").gain(0.6)

// Random twinkles~
_$: note("e5*16,b4*7,e2*5").swingBy(0.1,8).late(0.005).fm(rand).gain(0.9).clip(0.9).room(0.2)
-->
</strudel-editor>

## More?

Maybe that's plenty of tips for now -- I can feel myself veering off track. But I might write more about Strudel in the future. Let me know what you think [on Bluesky](https://bsky.app/profile/chordbug.bsky.social) or email me (`lynn@foldr.moe`).

